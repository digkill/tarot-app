# Same value as the main backend: the game server accepts its access tokens.
JWT_SECRET=
# The main backend's database (it owns the users table).
DATABASE_URL=
ARCANA_ADDR=:8090
ARCANA_TURN_SECONDS=40
ARCANA_MULLIGAN_SECONDS=30
ARCANA_RECONNECT_SECONDS=45
